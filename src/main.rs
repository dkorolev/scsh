//! scsh — Scoped Skills Helper.
//!
//! A single, standard-library-only Rust binary at the repository root: it preflights a git
//! repo and runs that repo's scoped agent skills in throwaway containers. This is the crate
//! scaffold; the parser, runtime, live UI, and commands arrive in the commits that follow.

fn main() {
  std::process::exit(0);
}
